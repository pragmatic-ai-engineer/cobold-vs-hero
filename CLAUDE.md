# Claude Instructions

Follow `AGENTS.md`.

Use the shared runbook in `shared-ai-runbook/` when shaping tasks, designing a
loop contract, or preparing review notes.

Use `delivery-pack/` for participant-facing requirements, HLD/LLD, ADRs,
prompts, workflow, and decision records. Use `manual-api/` for Bruno smoke
checks.
