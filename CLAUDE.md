# Claude Instructions

Follow `AGENTS.md`.

Use the shared runbook in `shared-ai-runbook/` when shaping tasks, designing a
loop contract, or preparing review notes.

Use `delivery-pack/` for participant-facing product, design, testing, delivery,
and prompt artifacts. Use `manual-api/` for Bruno smoke checks.
