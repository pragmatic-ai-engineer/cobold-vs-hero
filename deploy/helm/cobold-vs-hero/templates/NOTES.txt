Cobold vs Hero has been installed.

Check rollout status:

  kubectl -n {{ .Release.Namespace }} get pods

If using the default no-host ingress, open the server IP once DNS/firewall
points at this cluster.
