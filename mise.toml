[tools]
java = "zulu-17"
node = "24"

[tasks."be:start"]
description = "Run the Spring Boot API on http://localhost:8080"
dir = "backend"
run = "./gradlew bootRun"

[tasks."be:test"]
description = "Run backend tests"
dir = "backend"
run = "./gradlew test"

[tasks."fe:install"]
description = "Install Angular dependencies"
dir = "frontend"
run = "npm install --registry=https://registry.npmjs.org"

[tasks."fe:ci-install"]
description = "Install Angular dependencies for CI"
dir = "frontend"
run = "npm ci --registry=https://registry.npmjs.org"

[tasks."fe:start"]
description = "Run the Angular app on http://localhost:4200"
dir = "frontend"
run = "npm start"

[tasks."fe:build"]
description = "Build the Angular app"
dir = "frontend"
run = "npm run build"

[tasks.verify]
description = "Run the demo verification gates"
run = ["mise run be:test", "mise run fe:build"]

[tasks.ci]
description = "Run the GitHub Actions verification gates"
run = ["mise run fe:ci-install", "mise run be:test", "mise run fe:build"]
